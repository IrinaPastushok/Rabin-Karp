#pragma once
#include <string>
using namespace std;
void algoritm(const string cmp);
long double hash_gorner(const string str, int len_of_range, int start, long double res_of_last_str);
void output(const string str, int shift, int tmp, int len_of_range);

